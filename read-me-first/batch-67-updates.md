# Batch 67 updates

This batch completes the structured-data layer on the working build.

## Main changes
- Added missing JSON-LD to the Sources page.
- Added missing JSON-LD to the statutory redundancy, holiday entitlement and notice rules guide pages.
- Added breadcrumb and webpage schema to Contact and How we check pages.
- Added a WebPage schema block to the Guides hub.
- Added a visible FAQ section to the notice rules guide so the FAQ schema matches the public page content.
- Updated the guide/tool template placeholders to note the intended schema shape for future generator work.

## Why this batch matters
The previous build already had a good schema base on many core tools and hubs, but several public pages still had no JSON-LD at all. This batch closes that gap so the next checkpoint carries a more complete structured-data layer across trust pages, hubs and rule guides.
